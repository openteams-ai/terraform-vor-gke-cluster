
version: ">= 0.16"

formatter: markdown

header-from: docs/components/network-header.md

sections:
  show:
    - header
    - requirements
    - providers
    - modules
    - resources
    - inputs
    - outputs

content: |-
  ## Technical Reference - Network Component

  > This is the auto-generated technical reference for the network component.
  > For architectural guidance and security explanations, see the [network configuration guide](network-header.md).

  ## Resources

  This component manages the following resources:

  {{ range .Module.Resources }}
  - {{ .GetMode }}.{{ .Spec }} ({{ .Position.Filename }}#{{ .Position.Line }})
  {{- end }}

  {{ .Requirements }}
  {{ .Providers }}
  {{ .Modules }}
  {{ .Resources }}
  {{ .Inputs }}
  {{ .Outputs }}

output:
  file: docs/components/network.md
  mode: replace

settings:
  anchor: true
  color: true
  default: true
  description: false
  escape: true
  hide-empty: false
  html: true
  indent: 2
  lockfile: true
  read-comments: true
  required: true
  sensitive: true
  type: true
